import { write } from "@bedrock/io";
// import { transpile } from "@bedrock/mcfl/transpiler";
import { getResource } from "@bedrock/resource";

console.log(getResource("rp:manifest"));
// transpile("./src/script.glsl");
// write.json("rp:manifest.json", getResource("rp:manifest"));
// write.json("bp:manifest.json", getResource("bp:manifest"));
